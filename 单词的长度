#include <iostream>
#include <string>
#include <vector>
#include <sstream>
using namespace std;
int main(){
    string s;
    getline(cin, s);
    vector<string>(a);
    stringstream ss(s);
    while (ss >> s) {
        a.push_back(s);
    }
    cout<< a[0].length();
    a.erase(a.begin());
    for( const string &sss : a){
        cout<<","<< sss.length();
    }
    return 0;
}
